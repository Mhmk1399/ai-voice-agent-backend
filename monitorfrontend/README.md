# SuccessVan Agent Monitor

Vite/React test monitor for the SuccessVan voice-agent API.

## Commands

```bash
npm install
npm run dev
npm run build
```

The dev server proxies `/agent`, `/debug`, and `/health` to the API on port `4010`.
